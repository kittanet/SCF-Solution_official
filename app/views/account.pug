extends layout
block content
    center
        h2 Wallet
        br
        h4 Company Code: #{company.CompanyCode}
        h4 Name Thai: #{company.NameThai}
        h4 Name English: #{company.NameEnglish}
        h4 Address: #{accounts}
        h4 Balance ETH:	#{balance.eth} Ether 
        h4 Balance Token: #{balance.token} BBL
        br
        a.btn.btn-secondary(href='/wallets/account', role='button') Back